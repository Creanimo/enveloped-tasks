export declare const staticServer: (root: any, opts: {
    logLevel: number;
    injectedCode: string;
}) => (req: any, res: any, next: any) => any;
/**
 * Rewrite request URL and pass it back to the static handler.
 * @param staticHandler {function} Next handler
 * @param file {string} Path to the entry point file
 */
export declare const entryPoint: (staticHandler: any, file: any) => (req: any, res: any, next: any) => void;
